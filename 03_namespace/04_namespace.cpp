#include <iostream>

int main() {
    // using namespace for only cout (not for other packages in stander library
    using std::cout;
    using std::endl;

    cout << "hello_world" << endl;
    return 0;
}